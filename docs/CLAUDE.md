# docs — Claude 工作说明

## 作用

这里是仓库的长期说明层，目标是让文档反映真实系统，而不是替代源码。

## 进入此目录时优先关注

- 文档是否与当前实现一致
- 新内容属于安装、结构、配置、边界还是规划中的哪一类
- 是否需要补充链接到 `xdl/`、`train/`、`tests/`
- 内容应留在 `docs/` 还是更适合放到 `research/`

## 本地约束

- 先验证事实，再写结论
- 避免跨文档重复解释同一件事
- 文档变更尽量带上上下文，不只改一句口号式描述
