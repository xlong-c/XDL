# xdl/metric — 评估指标子模块

## 目录职责

- 提供训练和验证阶段使用的评估指标实现
- 通过注册系统支持配置化构建

## 当前内容

- `metrics.py`：分类与回归常用指标

## 修改约束

- 新增 metric 后必须在 `__init__.py` 中集中注册
- 指标应明确 state、`update()`、`compute()` 语义
- 注意 batch 累积与多次调用后的状态一致性
- 输入格式假设、阈值策略、top-k 逻辑要写清楚

## 验证建议

- 修改后优先跑 `tests/metric/`
- 覆盖状态累计、清零与边界样本
