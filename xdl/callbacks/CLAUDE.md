# xdl/callbacks — Claude 工作说明

## 作用

这里是训练回调扩展层，负责在不侵入主循环的前提下接入日志、监控、保存和可视化。

## 工作重点

- 回调优先级和触发时机是否符合预期
- 可选依赖是否优雅降级
- 回调是否越界修改训练状态

## 本地约束

- 新回调先看 `base.py` 和 `callback_list.py`
- 观察者职责优先，避免把业务主逻辑塞进回调
