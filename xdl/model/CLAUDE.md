# xdl/model — Claude 工作说明

## 作用

这里是模型实现层，负责把具体网络结构接入 XDL registry。

## 工作重点

- 模型实现是否遵循现有注册和导出模式
- 输入输出形状、可配置参数和依赖假设是否清楚
- 是否有本应放在 `train/` 或 `trainer/` 的训练逻辑混入模型文件

## 本地约束

- 新模型先查 `__init__.py` 的集中注册模式
- 结构复用优先贴近现有文件组织
