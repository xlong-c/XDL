# config — Claude 工作说明

## 作用

这里存放仓库级训练与运行配置，不是核心实现代码。

## 进入此目录时优先检查

- 配置字段是否与 `xdl/config/` 中的 dataclass、builder 对齐
- 示例是否仍可被训练入口直接消费
- 修改是否需要同步 `tests/config/`

## 本地约束

- 配置优先使用 YAML/JSON
- 不保留私有路径、临时实验残留、凭据
- 示例配置应尽量可复用、可读、可直接拷贝
